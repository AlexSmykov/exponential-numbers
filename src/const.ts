import { ExponentNumber } from 'src/class/exponent-number.class';

export const VALUE_EXPONENT_LIMIT = 100;
export const VALUE_EXPONENT_DIFFERENCE_LIMIT = 12;
export const ZERO_NUMBER = new ExponentNumber(0, 0);
